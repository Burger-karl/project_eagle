# ─── Core Framework ───────────────────────────────────────────
Django==4.2.16
djangorestframework==3.15.2
django-cors-headers==4.3.1
django-filter==24.1

# ─── MSSQL Database (Sage 200 backup is SQL Server) ──────────
mssql-django==1.4          # Django backend for Microsoft SQL Server
pyodbc==5.1.0              # ODBC driver for direct MSSQL queries

# ─── Async Task Queue (high-volume invoice processing) ────────
celery==5.3.6
redis==5.0.1               # Celery broker & result backend
django-celery-beat==2.6.0  # Scheduled periodic tasks
django-celery-results==2.5.1

# ─── Multi-tenancy (one instance, many clients) ───────────────
django-tenants==3.6.1

# ─── HTTP client (FIRS API, Sage 200 REST API calls) ─────────
requests==2.32.3
urllib3==2.2.1

# ─── Cryptography (ECDSA invoice signing for FIRS) ────────────
cryptography==42.0.8
ecdsa==0.19.0

# ─── Data & Serialisation ─────────────────────────────────────
lxml==5.2.2                # XML/UBL invoice building
python-dateutil==2.9.0

# ─── Environment & Config ─────────────────────────────────────
python-dotenv==1.0.1
django-environ==0.11.2

# ─── Logging & Monitoring ─────────────────────────────────────
sentry-sdk==2.3.1          # Error tracking in production

# ─── Security ─────────────────────────────────────────────────
django-ratelimit==4.1.0
PyJWT==2.8.0

# ─── API Docs ─────────────────────────────────────────────────
drf-spectacular==0.27.2    # Auto OpenAPI/Swagger docs

# ─── Testing ──────────────────────────────────────────────────
pytest==8.2.1
pytest-django==4.8.0
pytest-mock==3.14.0
factory-boy==3.3.0
responses==0.25.3          # Mock HTTP calls in tests

# ─── Dev Tools (remove in production) ─────────────────────────
django-debug-toolbar==4.3.0
ipython==8.24.0
